{
  "private": true,
  "name": "electron-notes-app",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run start\" \"npm --prefix frontend run start-electron\"",
    "clean": "node scripts/clean-node-modules.js",
    "install": "node scripts/install-all.js"
  },
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
