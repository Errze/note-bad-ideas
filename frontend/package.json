{
  "name": "electronapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --disable-gpu-sandbox",
    "react-start": "cd renderer && npm start",
    "build": "cd renderer && npm run build",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-electron\"",
    "start-backend": "cd ../backend && npm start",
    "start-electron": "npm run build && electron . --disable-gpu-sandbox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^38.2.1"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-markdown": "^9.0.0"
  }
}
